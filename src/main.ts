/**
 * Weather App - Entry Point
 * Importa gli stili e inizializza l'applicazione meteo.
 */

import './styles.css';
import { initializeWeatherApp } from './app.js';

// Inizializza l'app meteo al caricamento della pagina
initializeWeatherApp();
